---
# Secret for sensitive data
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: punju-university
  labels:
    app: backend
    component: secret
type: Opaque
stringData:
  JWT_SECRET: "your-secret-key-here-change-in-production"
  MONGODB_USERNAME: ""
  MONGODB_PASSWORD: ""

---
# Docker Registry Secret (uncomment and update if using private registry)
# apiVersion: v1
# kind: Secret
# metadata:
#   name: registry-credentials
#   namespace: punju-university
# type: kubernetes.io/dockercfg
# data:
#   .dockercfg: <base64-encoded-dockercfg>
